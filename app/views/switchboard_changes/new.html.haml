- title "Ändra adress"
%section.box.form#switchboard-changes
  %h1.box-title= h1
  .box-content
    %p.instruction
      = "Dina ändringar kommer att sändas till televäxeln."
      = "När de uppdaterat uppgifterna i telefonisystemet kommer ändringarna att synas i kontaktboken."

    = form_tag :switchboard_changes, method: :post, class: "form-horizontal" do
      / .control-group
      /   = label_tag :room, "Rumsnummer:", class: "control-label"
      /   .controls
      /     = text_field_tag :room, current_user.room, class: 'input-wide'

      .control-group
        = label_tag :address, "Adress:", class: "control-label"
        .controls
          = text_field_tag :address, current_user.address, class: 'input-wide', placeholder: "Skriv och välj ur förslagslistan"

      .control-group
        = label_tag :comment, "Kommentar:", class: "control-label"
        .controls
          = text_area_tag :comment, nil, class: 'input-wide', rows: 7

      .control-group
        .controls
          = submit_tag "Sänd", class: "btn btn-primary"
          = link_to t('actions.cancel'), user_path(current_user.username), class: "btn"
