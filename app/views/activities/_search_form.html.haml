= form_tag(activities_search_path, method: "get", id: "search-activity", class: "basic", "data-path" => activities_path) do
  .form-group
    = label_tag "term", "Sök aktivitet: ", class: "control-label"
    .controls
      .input-append
        = text_field_tag "term", params[:term]
        = submit_tag "Sök", { class: "btn btn-default", name: nil }
