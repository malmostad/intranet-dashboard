= form_tag(skills_search_path, method: "get", id: "search-skill", class: "form-horizontal") do
  .control-group
    = label_tag "q", "Sök kunskapsområde: ", class: "control-label"
    .controls
      .input-append
        = text_field_tag "q", params[:q]
        = submit_tag "Sök", { class: "btn", name: nil }
