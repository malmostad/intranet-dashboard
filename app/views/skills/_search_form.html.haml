= form_tag(skills_search_path, method: "get", id: "search-skill", class: "form-horizontal", "data-path" => skills_path) do
  .control-group
    = label_tag "term", "Sök kunskapsområde: ", class: "control-label"
    .controls
      .input-append
        = text_field_tag "term", params[:term]
        = submit_tag "Sök", { class: "btn", name: nil }
