# Autocomplete search for skills
jQuery ->
  $("#search-skill #term").focus().autocomplete
    source: '<%= asset_path("/skills/search") %>',
    minLength: 2,
    autoFocus: true,
    select: (event, ui) ->
      document.location = '<%= asset_path("/skills/") %>' + ui.item.id + '/edit'
